const { ax, mock } = require("./aa-lib.js")

module.exports = {

  GetUser(postData) {
    return ax("/", postData)

    /* return mock(0, {
      value: "first user",
    }) */
  },

  Login(p) {
    return mock(0, {
      id: "aoehunaoheunaohen_id",
      display_name: "Alice",
      token: "oeuthnthoeatnhuno_token",
      username: p.username,
      password: p.password,
    })
  },

}
