const { ax, mock } = require("./aa-lib.js")

function GetUser() {
  return ax.post("/a").then((resp) => {
      return resp.data
    })
    .catch((err) => {
      console.log("nice", err)
      return err
    })

  /* return mock(0, {
    value: "first user",
  }) */
}

module.exports = {
  GetUser,
}
