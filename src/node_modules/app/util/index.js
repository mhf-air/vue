// app shared code

function addDefaultMutations(actions, mutations) {
  for (let k in mutations) {
    if (!actions.hasOwnProperty(k)) {
      actions[k] = ({ commit }, s) => {
        commit(k, s)
      }
    }
  }
}

module.exports = {
  addDefaultMutations,
}
