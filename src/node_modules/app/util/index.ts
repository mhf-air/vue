// app shared code

export function addDefaultMutations(actions: any, mutations: any) {
  for (let k in mutations) {
    if (!actions.hasOwnProperty(k)) {
      // actions[k] = ({ commit }, s) => {
      actions[k] = (a: any, s:any) => {
        a.commit(k, s)
      }
    }
  }
}

export const db = {
  set(name: string, value: any) {
    if (!name) {
      return
    }
    if (typeof value !== "string") {
      value = JSON.stringify(value)
    }
    window.localStorage.setItem(name, value)
  },
  get(name: string) {
    if (!name) {
      return
    }
    window.localStorage.getItem(name)
  },
  remove(name: string) {
    if (!name) {
      return
    }
    window.localStorage.removeItem(name)
  },
  clear() {
    window.localStorage.clear()
  },
}

/* module.exports = {
  addDefaultMutations,
  db,
} */
